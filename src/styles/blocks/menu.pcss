.menu__list {
  display: flex;
}
.menu__item {transition: color $transition-duration;
  margin-right: 60px; 
  font-size: 1rem;
  font-weight: normal;
&:hover {
  color: $main-color;}
&:last-child {
  margin-right: 0;
}
}